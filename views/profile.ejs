<%- include('bootheader') -%>
<%- include('menubar') -%>
    <div class="jumbotron" style="text-align:center; font-size:20px">

      <link href="https://fonts.googleapis.com/css?family=Dosis&display=swap" rel="stylesheet">
      <style>
      *{
      font-family: 'Dosis', sans-serif;
      }
      .jumbotron {
         /*background: rgb(200, 54, 54); /* This is for ie8 and below */
         background: rgba(255, 255, 255, 0.2);*/
      }
      </style>
        <h1><%= user.userName %>'s tempo profile</h1>
        <img src="<%= user.profilePicURL%>" width="200">
        <hr>
        <p> Welcome <%= user.googlename %>
            with email <%= user.googleemail %>
            You are logged in to tempo.
        </p>
        <p> <%= user.zipcode %> <%= user.city %> <%= user.state %> </p>
        <p> Your wakeup music is <b><%= user.wakeup %></b> </p>
        <p> Last updated on <%= user.lastUpdate %> </p>
<a href="/editProfile">edit profile</a>
    </div>

<%- include('bootfooter') -%>
